#include<stdio.h>
void main()
{
    int *ptr;
    float *ptr1;
    char *ch;
    double *d;
    printf("size of in ptr :%d\n",sizeof(ptr));
    printf("size of in ptr :%d\n",sizeof(ptr1));
    printf("size of in ptr :%d\n",sizeof(ch));
    printf("size of in ptr :%d\n",sizeof(d));
    
}